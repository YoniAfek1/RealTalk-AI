transformers==4.36.2
# Use a torch version available on Vercel's Python images
torch==2.2.0
# Use a version with prebuilt wheels to avoid needing cmake on Vercel
sentencepiece==0.2.0
numpy>=1.20.0
omegaconf>=2.3.0 